# Test classpath wins over src/main/resources, so the suite stays quiet: the
# library logs the SQL it builds at debug, which is 84 tests' worth of noise.
# Raise this to debug when a test's generated SQL is what you need to see.
org.slf4j.simpleLogger.defaultLogLevel=warn
